{
  "name": "svite-minimal",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "dev": "svite",
    "build": "svite build",
    "deploy": "npx gh-pages -d dist"
  },
  "devDependencies": {
    "svelte": "^3.27.0",
    "svelte-hmr": "^0.11.0",
    "svite": "^0.7.1"
  }
}
